```typescript jsx
// proID/frontend/src/App.tsx
import './App.css'; // Keep basic styling
import RegisterForm from './components/RegisterForm'; // Import the new component

function App() {
  // Later, we'll add state here to manage login status and routing
  return (
    <>
      <h1>proID Application</h1>
      <div>
        <RegisterForm />
        {/* We can add a Login Form component here later */}
      </div>
    </>
  )
}

export default App;
```